import { ClassFilterPipe } from './class-filter.pipe';

describe('ClassFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
